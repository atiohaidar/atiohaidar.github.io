# Flutter web app - ensure static assets are served correctly
/flutter/*.js /flutter/:splat 200
/flutter/*.json /flutter/:splat 200
/flutter/*.wasm /flutter/:splat 200
/flutter/assets/* /flutter/assets/:splat 200
/flutter/icons/* /flutter/icons/:splat 200
/flutter/canvaskit/* /flutter/canvaskit/:splat 200

# Flutter app - all other routes go to index.html (SPA fallback)
/flutter/* /flutter/index.html 200

# Expo web app - ensure static assets are served correctly
/_expo/static/js/web/*.js /_expo/static/js/web/:splat 200
/_expo/static/* /_expo/static/:splat 200
/_expo/* /_expo/:splat 200
/expo/*.js /expo/:splat 200
/expo/*.json /expo/:splat 200
/expo/assets/* /expo/assets/:splat 200
/expo/_expo/* /expo/_expo/:splat 200

# Expo app - all other routes go to index.html (SPA fallback)
/expo/* /expo/index.html 200

# React frontend - SPA fallback for root
/* /index.html 200
